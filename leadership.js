document.addEventListener('DOMContentLoaded', () => {
  // The leadership functionality is implemented in app.js
  // This file ensures the page initializes properly
  if (typeof initLeadershipPage === 'function') {
    initLeadershipPage();
  }
});
